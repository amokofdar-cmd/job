# Security Rules

- Never store secrets in code.
- Never commit .env files.
- Validate all uploads.
- Enforce server-side auth checks.
- Use least privilege for credentials and integrations.
- Do not expose private user data to models unless required.
- Resume tailoring must not invent candidate qualifications.